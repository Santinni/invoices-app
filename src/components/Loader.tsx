import Icon from '../icons';
import '../style/Loader.scss';

const Loader = () => {
    return (
        <div className="loader">
            <Icon name="preloader" />
        </div>
    );
};

export default Loader;